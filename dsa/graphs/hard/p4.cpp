// https://acm.timus.ru/problem.aspx?space=1&num=1003
// Parity

#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
#define pb push_back

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int len, q, start, end;
    string ans;
    cin >> len >> q;
    for (int i=0; i<q; i++)
    {
        cin >> start >> end >> ans;
    }
    return 0;
}