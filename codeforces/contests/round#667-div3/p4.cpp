#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    int t;
    cin>>t;
    while (t-->0) {
        ll num, sum;
        cin>>num>>s;
        string s = num.to_string();
        pow(10, s.length()-1)
    }
    return 0;
}